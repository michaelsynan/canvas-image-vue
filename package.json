{
  "name": "canvas-image-grid",
  "version": "0.5.0-beta.1",
  "description": "Divide images into canvas elements based on desired number of rows and columns. ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vue-cli-service build --target lib --name my-component src/components/CanvasGrid.vue"
  },
  "author": "Michael Synan",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.11.0",
    "vue": "^3.2.47"
  },
  "devDependencies": {
    "@vue/cli-service": "^5.0.8"
  }
}
